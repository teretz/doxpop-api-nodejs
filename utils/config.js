// Configuration Utility

const config = {
  PORT: process.env.PORT || 3000,
  API_USERNAME: process.env.API_USERNAME || 'demo',
  API_PASSWORD: process.env.API_PASSWORD || 'demo',
  // Add other configuration variables here
};

module.exports = config;